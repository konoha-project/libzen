class Person {
	field name: String;
	field age: int;
	field isChild: Func<boolean, Person>;
}

class Student extends Person {
	field SchoolName: String;
	field hasSchalar = true;
}

function isChild(p: Person) {
	return p.age <= 20;
}
